# Admin API

## Registry
- `Registry` — Admin model registry
- `register(model)` — Register a model for admin
- `unregister(model)` — Unregister a model
- `getRegistered()` — Get all registered models
