/// zypher admin — auto-generated CRUD interface for registered models.
const std = @import("std");

// Phase 8 implementations will add registry.zig, views.zig

test {
    std.testing.refAllDecls(@This());
}
