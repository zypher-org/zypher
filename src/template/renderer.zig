/// zypher template engine — server-side HTML rendering with auto-escaping.
const std = @import("std");

// Phase 4 implementations will add lexer.zig, parser.zig, renderer.zig, filters.zig

test {
    std.testing.refAllDecls(@This());
}
